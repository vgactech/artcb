# CONFIGURATION — ARTCB MVP Avancé

**Horodatage :** 2026-07-04T23:05:00Z  
**Statut :** Q-002→Q-016 résolues — voir DECISIONS D-019→D-022

---

## 1. Variables d'environnement requises

| Variable | Obligatoire | Défaut | Description |
|----------|-------------|--------|-------------|
| `ARTCB_DEBUG` | Non | `true` | Mode DEBUG (PROTOCOLE : actif par défaut) |
| `ARTCB_LOG_LEVEL` | Non | `DEBUG` | Niveau logs : DEBUG, INFO, WARNING, ERROR |
| `ARTCB_LOG_DIR` | Non | `./logs` | Dossier logs JSON |
| `ARTCB_REPORTS_DIR` | Non | `./rapports` | Dossier rapports |
| `ARTCB_DATA_DIR` | Non | `./data` | Données persistantes (chaîne, graphes) |
| `ARTCB_HOST` | Non | `0.0.0.0` | Host API FastAPI |
| `ARTCB_PORT` | Non | `8000` | Port API |
| `ARTCB_CHAIN_KEY_PATH` | Non | `./data/chain.key` | Clé Ed25519 chaîne privée |
| `ARTCB_ENCRYPTION_KEY` | Non | auto-généré | Clé AES-256-GCM (fichier local) |
| `ARTCB_POL_ALPHA` | Non | `0.4` | Poids Δcompression dans PoL |
| `ARTCB_POL_BETA` | Non | `0.3` | Poids validation_rate |
| `ARTCB_POL_GAMMA` | Non | `0.3` | Poids retrieval_accuracy |
| `ARTCB_POL_THRESHOLD` | Non | `0.6` | Seuil acceptation bloc |
| `ARTCB_NETWORK` | Non | `local` | Réseau : `local`, `devnet`, `federated` |
| `ARTCB_P2P_PORT` | Non | `18444` | Port P2P artcb-devnet (Phase 3) |
| `ARTCB_BLOCK_REWARD` | Non | `50` | Block reward initial ARTCB (TOKENOMICS) |
| `ARTCB_HALVING_INTERVAL` | Non | `210000` | Blocs entre halvings |
| `ARTCB_MAX_SUPPLY` | Non | `21000000` | Supply max par ledger (pARTCB / pubARTCB) |
| `ARTCB_DEMO_BOOK_PDF` | Non | `data/fixtures/wailly_le_roi_de_l_inconnu.pdf` | Livre démo Q-006 — 100 % |
| `BOB_API_KEY` | Non* | — | **Canonique** — LLM path B (IBM Bob CLI) |
| `OPENROUTER_API_KEY` | Non* | — | **Alias** de `BOB_API_KEY` — hackathon ne fournit pas OpenRouter |
| `BOB_API_BASE` | Non | `https://api.us-east.bob.ibm.com` | Base URL Bob |
| `ARTCB_LLM_ENABLED` | Non | `false` | Activer path B si clé Bob présente |
| `GRADIUM_API_KEY` | Non | — | TTS voix (Q-004 = oui) |
| `GRADIUM_API_URL` | Non | `https://api.gradium.ai` | Base URL Gradium |
| `GOOGLE_AI_API_KEY` | Non* | — | **Google AI (Gemini)** — provider `google_ai` dans Intégrations |
| `GOOGLE_AI_MODEL` | Non | `gemini-1.5-flash` | Modèle Gemini par défaut (gemini-1.5-flash / gemini-1.5-pro / gemini-2.0-flash) |

\* Path B : `BOB_API_KEY` **ou** `OPENROUTER_API_KEY` (même clé). Rule-based (path A) fonctionne **sans** clé.
\*\* Google AI : aucun package pip requis — API REST v1beta `generativelanguage.googleapis.com`. Obtenir une clé sur https://aistudio.google.com/app/apikey

---

## 2. Fichier `.env.example` (à créer en Phase 1)

```bash
# ARTCB Configuration — copier vers .env et remplir
ARTCB_DEBUG=true
ARTCB_LOG_LEVEL=DEBUG
ARTCB_LOG_DIR=./logs
ARTCB_REPORTS_DIR=./rapports
ARTCB_DATA_DIR=./data
ARTCB_HOST=0.0.0.0
ARTCB_PORT=8000
ARTCB_CHAIN_KEY_PATH=./data/chain.key
ARTCB_POL_ALPHA=0.4
ARTCB_POL_BETA=0.3
ARTCB_POL_GAMMA=0.3
ARTCB_POL_THRESHOLD=0.6

# Optionnel — encodage LLM
# OPENROUTER_API_KEY=sk-or-v1-...
# OPENROUTER_MODEL=anthropic/claude-3.5-sonnet

# Optionnel — Google AI (Gemini) — aucun pip requis
# GOOGLE_AI_API_KEY=AIza...
# GOOGLE_AI_MODEL=gemini-1.5-flash
# → Obtenir clé : https://aistudio.google.com/app/apikey
# → Dans Intégrations frontend : provider = google_ai, api_key = valeur ci-dessus

# Optionnel — voix P1
# GRADIUM_API_KEY=...
```

**Sécurité :** `.env` doit être dans `.gitignore` — jamais commité.

---

## 3. Dépendances Python (requirements.txt — spec)

```
# Core
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-multipart>=0.0.9

# Graphe & mémoire
networkx>=3.3
numpy>=1.26.0
faiss-cpu>=1.8.0

# Crypto
cryptography>=42.0.0
pynacl>=1.5.0

# Utilitaires
python-dotenv>=1.0.0
httpx>=0.27.0
websockets>=12.0

# NLP fallback (sans LLM)
spacy>=3.7.0
# python -m spacy download fr_core_news_sm

# Tests
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# Qualité
ruff>=0.4.0
mypy>=1.10.0
```

---

## 4. Dépendances Frontend (package.json — spec)

```json
{
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "cytoscape": "^3.28.0",
    "cytoscape-react": "^2.0.0",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "vite": "^5.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.5.0",
    "@types/react": "^18.3.0"
  }
}
```

---

## 5. Dépendances système

| Outil | Version min | Usage |
|-------|-------------|-------|
| Python | 3.11+ | Backend |
| Node.js | 20+ | Frontend |
| GCC / Clang | C11 | libartcb_chain (Phase 3) |
| Make | — | Compilation C |
| Docker | 24+ | Démo reproductible (optionnel) |
| Git | 2.40+ | Versionnement |

---

## 6. Ports réseau

| Service | Port | Protocole |
|---------|------|-----------|
| API FastAPI | 8000 | HTTP |
| WebSocket | 8000/ws | WS |
| Frontend Vite dev | 5173 | HTTP |
| Frontend prod | 3000 | HTTP (via nginx/docker) |

---

## 7. Structure données locales (`ARTCB_DATA_DIR`)

```
data/
├── chain.key              # Clé Ed25519 (auto-générée)
├── encryption.key         # Clé AES (auto-générée)
├── chain/
│   ├── blocks.jsonl       # Blocs blockchain
│   └── index.db           # SQLite index (optionnel)
├── graphs/
│   └── {uuid}.json        # Graphes IR persistés
├── rtleg/
│   └── events.jsonl       # Journal RT-LEG
└── vectors/
    └── {graph_id}.faiss   # Index vectoriel par graphe
```

---

## 8. Configuration Docker Compose (spec)

```yaml
services:
  api:
    build: .
    ports: ["8000:8000"]
    volumes: ["./data:/app/data", "./logs:/app/logs"]
    env_file: .env
  frontend:
    build: ./frontend
    ports: ["5173:5173"]
    depends_on: [api]
```

---

## 9. Clés API — clarification (blockchain décentralisée)

### Principe

**La blockchain ARTCB (C) ne contient et ne demande AUCUNE clé API.**

Les clés ci-dessous sont **optionnelles**, pour services **externes** uniquement, stockées **localement** dans `.env` (gitignoré, jamais commitées, jamais sur la chaîne).

| Variable | Service externe | Obligatoire ? | Sur blockchain ? |
|----------|----------------|---------------|------------------|
| `OPENROUTER_API_KEY` | Alias **Bob CLI** (même clé) — hackathon **ne fournit pas** OpenRouter | Non — path B optionnel | ❌ Jamais |
| `BOB_API_KEY` | IBM Bob CLI — LLM path B (canonique) | Non — rule-based par défaut | ❌ Jamais |
| `GRADIUM_API_KEY` | Synthèse vocale hackathon | Non — skip par défaut | ❌ Jamais |

### Où vont les clés si tu en fournis ?

```
lvx/
├── .env              ← TON fichier local (gitignoré)
├── .env.example      ← Modèle sans secrets (dans le repo)
├── data/chain/       ← Blockchain C (propriétaire VGACTech — voir LICENCE_ARTCB.md)
└── src/c/            ← Core blockchain C
```

Chaque clone du repo public fonctionne **sans clé**. Les clés sont un choix **personnel local** pour activer des bonus (LLM, voix).

### Éléments utilisateur — statut

| Élément | Statut | Responsable |
|---------|--------|-------------|
| Validation CDC v1.1 | ✅ | Agent |
| Phase 1 IR Engine | ✅ | Agent |
| `OPENROUTER_API_KEY` / `BOB_API_KEY` | ✅ Q-002 + Q-009 D — `.env` local Bob CLI | Toi |
| `GRADIUM_API_KEY` | ✅ Q-004 oui — `.env` local | Toi |
| Équipe hackathon | Hors scope agent | Toi |
| Merge → main à chaque push | ✅ Règle D-001 | Agent |
| Crédits Cursor formulaire | ⏳ | Toi |

---

## GO-J (2026-09-07) — ARTCB_NODE_PUBLIC_URL nœuds distants

À configurer dans Doppler **sur chaque nœud** (SSH ou Doppler Dashboard) :

| Nœud | IP | Variable | Valeur à définir |
|------|-----|----------|-----------------|
| OVH2 | 151.80.107.29 | `ARTCB_NODE_PUBLIC_URL` | `http://151.80.107.29:8000` |
| AWS3 | 51.44.222.232 | `ARTCB_NODE_PUBLIC_URL` | `http://51.44.222.232:8000` |
| OVH4 | 91.134.45.8  | `ARTCB_NODE_PUBLIC_URL` | `http://91.134.45.8:8000` |

Sans cette variable, `advertised_base_url` retourne `http://localhost:8000` (bug d'annonce P2P).
Après redémarrage du service sur chaque nœud, vérifier avec :
`curl http://<IP>:8000/api/v1/p2p/status | grep advertised_base_url`

OVH1 (`152.228.144.34`) est déjà correct (variable présente dans son env).
