# Rapport 259 — Failover honnête OVH4 (arrêt + bloc + rattrapage)

**Date :** 2026-09-08T00:41Z → 00:44Z  
**Contact :** `official@artcb.space`  
**GO opérateur :** « Arrête le node et go ! » — arrêt d’**un** nœud officiel, continuité, nouveau bloc, replica, redémarrage, rattrapage.  
**Pas de wipe.** Pas de `install.sh` / genesis / init-node / rescue. GO-E produce **off**.

`origin/main` pendant la boucle = `/health` ×4 = **`8e2be0b4ecb7a696312682b9ae2e2d9c0db2e9f0`**.

Rien n’est inventé. Tous les hashes, hauteurs, RTT et `wc -l` ci-dessous sont mesurés.

---

## Ce que c’est / ce que ce n’est pas

| | |
|---|---|
| **C’est** | Failover **honnête-offline** : `systemctl stop artcb` sur **ovh-node-4** (`91.134.45.8`). Le livre reste sur disque. Les 3 autres continuent. Un bloc est gravé sur OVH1. Replica officiel 251 pousse le tip. OVH4 redémarre keep-book et rattrape. |
| **Ce n’est pas** | BFT d’append de bloc. View-change. Partition adversariale. Nœud byzantin (menteur, fork, double-sign). Vol de production GO-E. Settlement prepare/commit 188. |

`GET /consensus/liveness` reste **observe-only** (`scope=liveness_observe_only`, `not_block_append_bft=true`).  
`partitioned = reachable < Q`. Avec N=4, F=1, Q=3 : **3 joignables ⇒ `partitioned=false` `below_quorum=false`**. Q tient. Ce n’est pas un PASS BFT.

Nœud sacrifié : **OVH4**, pas OVH1 — OVH1 reste writer + coordinateur replica.

---

## 1. Avant arrêt (4 up)

| Nœud | SHA | height | tip | `chain_valid` | livre `wc -l` |
|---|---|---|---|---|---|
| OVH1 `152.228.144.34` | `8e2be0b4ecb7…` | **1077** | `b44ee5318a7f92d5…` | true | 1077 |
| OVH2 `151.80.107.29` | idem | **1077** | même | true | 1077 |
| AWS3 `51.44.222.232` | idem | **1077** | même | true | 1077 |
| OVH4 `91.134.45.8` | idem | **1077** | même | true | **1077** |

Liveness OVH1 : `n=4` `reachable=4` `q=3` `partitioned=false` `below_quorum=false`.

---

## 2. Arrêt OVH4

`sudo systemctl stop artcb` sur `ubuntu@91.134.45.8`. Service only.

| Mesure | Valeur |
|---|---|
| OVH4 `:8000` | `URLError` / connect refused |
| OVH4 `:8443` `/chain/status` | HTTP **502** (nginx up, uvicorn down) |
| OVH4 `data/chain/blocks.jsonl` | **1077** lignes — **non vidé** |
| OVH1 / OVH2 / AWS3 | height **1077**, tip `b44ee531…`, healthy |
| Liveness OVH1 + AWS3 | `n=4` `reachable=3` `partitioned=false` `below_quorum=false` |

Quorum Q=3 tient. Aucun second nœud arrêté.

---

## 3. Nouveau bloc pendant que OVH4 est down

`POST /ai/memo` HTTPS OVH1 `:8443` — HTTP **200**, dur_ns client **716519386**, server_ns **242685952**.

| Champ | Mesuré |
|---|---|
| `block_index` | **1077** |
| `block_hash` | `93c8b7c1dde1e79afa8f277f10f1705298c0beda5e9a7f3e5281a1f2d6cc51be` |
| `graph_id` | `ai_memo_666404b44bef` |
| tags | `259`, `failover`, `ovh4-down`, `memory` |
| KCG publish | `K_a32919c3db348082` HTTP 200 |
| OVH1 `/chain/status` après | height **1078**, `last_hash` `93c8b7c1…`, `chain_valid=true` |

OVH2 / AWS3 encore à 1077 `b44ee531…`. OVH4 toujours down, livre 1077.

---

## 4. Replica depuis OVH1 localhost (OVH4 down)

`POST /p2p/replica/run?include_files=false` sur `127.0.0.1:8000` OVH1 (bearer via SSH, jamais HTTP distant).  
`local_height=1078` `local_last_hash=93c8b7c1…`

| Pair | `ok` | avant → après | tip après | flux |
|---|---|---|---|---|
| ovh-node-2 | **true** | **1077 → 1078** | `93c8b7c1…` | chunk `1077-1077` pushed 1 imported 1 duplicates 0 ; plain **7874** wire **20569** ; encrypt **85.03 ms** http **368.42 ms** rtt **453.45 ms** |
| aws-node-3 | **true** | **1077 → 1078** | `93c8b7c1…` | same chunk ; encrypt **37.91 ms** http **250.35 ms** rtt **288.26 ms** |
| ovh-node-4 | **false** | — | — | `probe.error=ConnectError` port 8000 |

0 erreur sur les pairs joignables. OVH4 injoignable = attendu.

---

## 5. Restart OVH4 keep-book

`sudo systemctl start artcb`. Livre **avant** start : **1077**. Livre **après** start (avant replica) : **1077**.

| Mesure | Valeur |
|---|---|
| `systemctl is-active` | **active** |
| `:8000/health` | `healthy` SHA `8e2be0b4ecb7` branch `main` |
| `/chain/status` | height **1077** tip `b44ee5318a7f92d5…` `chain_valid=true` |

Le nœud est revenu avec **son** livre (tip d’avant le mémo 259). Pas de wipe, pas de genesis.

---

## 6. Replica catch-up (OVH4 up)

Même `POST /p2p/replica/run?include_files=false` depuis OVH1 localhost.

| Pair | `ok` | avant → après | note |
|---|---|---|---|
| ovh-node-2 | true | 1078 → 1078 | `nothing_to_send` |
| aws-node-3 | true | 1078 → 1078 | `nothing_to_send` |
| ovh-node-4 | **true** | **1077 → 1078** | tip `93c8b7c1…` ; pushed 1 imported 1 duplicates 0 ; plain **7874** wire **20569** ; encrypt **41.15 ms** http **358.54 ms** rtt **399.69 ms** |

SSH OVH4 `wc -l` après catch-up : **1078**. Service **active**.

---

## 7. Matrice 4-up après boucle (2026-09-08T00:44:09Z)

| Nœud | SHA | height | tip | `chain_valid` | `wc -l` | liveness |
|---|---|---|---|---|---|---|
| OVH1 | `8e2be0b4ecb7a696312682b9ae2e2d9c0db2e9f0` | **1078** | `93c8b7c1dde1e79afa8f277f10f1705298c0beda5e9a7f3e5281a1f2d6cc51be` | true | **1078** | `reachable=4` `partitioned=false` `q=3` |
| OVH2 | idem | **1078** | même | true | **1078** | idem |
| AWS3 | idem | **1078** | même | true | **1078** | idem |
| OVH4 | idem | **1078** | même | true | **1078** | idem |

`would_failover=false` partout (producteur non silencieux ; observe-only).  
OVH1 `blocks.bin` sidecar **15492** octets (était 7759 après 258 — append du bloc 1077, pas une conversion rétroactive).

Boucle fermée : **continuité 3/4 → bloc 1078 → replica 2+3 → restart D → catch-up D → égalité 4/4**.

---

## 8. Ce qui reste non démontré

- BFT sur `append_block` (view-change, 2f+1 signatures de bloc).
- Nœud **byzantin** (mauvais tip, double production, equivocation).
- Partition réseau (pas un `systemctl stop`) avec Q rompu (`reachable < 3`).
- Deux nœuds down simultanés.
- Vol de production GO-E (`would_failover` n’écrit pas de bloc).
- ConceptID universel (`voiture`/`car`/`coche` overlap 0 — inchangé).

188 reste settlement prepare/commit. 251 reste replica officiel, pas consensus.

---

## Interdit (rappel)

Wipe `blocks.jsonl`. `install.sh`. Genesis. `init-node`. Rescue. Afficher le token. Mélanger Doppler. Laisser OVH4 arrêté. Inventer un PASS BFT.
