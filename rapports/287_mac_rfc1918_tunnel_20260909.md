# Rapport 287 — RFC1918 `10.234.49.2` n’est pas un chemin cloud

**Date** : 2026-09-09T18:26:20Z  
**Live JSON** : `logs/287_mac_tunnel_live.json` stamp `20260909T182620Z`  
**origin/main / SHA ×4** : `75836c715fb47cf87d37365bd6d5b8e36d940c5e`  
**Livre OVH1** : height **1133**, tip `cc0bf8a1053c7ae75c3c00b0a4ebf72da311efbf1f13e3164a95f159fc7d71b3`

**Consigne opérateur** : joignable seulement depuis le LAN local ; un tunnel est nécessaire pour l’accès distant.

Ne pas inventer un `git_sha` health Mac. Ne pas afficher de token ngrok / Doppler / PEM.

---

## Verdict

| Chemin | Statut |
|--------|--------|
| TCP / SSH vers `10.234.49.2` depuis cet agent cloud | **NOT_REACHABLE** (RFC1918) |
| Tunnel public mesuré (`ARTCB_MAC_TUNNEL_SSH_HOST`) | **ABSENT** |
| Compte ngrok (`api.ngrok.com/tunnels` + `/endpoints`) | **0 / 0** |
| P2P `http://10.234.49.2:8001` | `private_or_loopback_forbidden` (hors `ARTCB_ALLOW_LOCAL_PEERS`) |
| ngrok démarré **sur cette VM cloud** | **non tenté** — n’atteindrait pas le Mac |
| `mac-node-local` dans `OFFICIAL_COMPUTE` | false |
| CERTIFIED_100 | false |

Un timeout LAN n’est pas un PASS SSH. Un hôte RFC1918 ou `*.local` collé dans `ARTCB_MAC_TUNNEL_SSH_HOST` est **refusé** (`tunnel_host_is_lan_only`).

## Ce que le code encode

- `NodeSpec.tunnel_required=True` sur `mac-node-local`
- `select_cloud_remote()` refuse `10.234.49.2` et `.local` comme « tunnel »
- Probe `scripts/run_live287_mac_tunnel.py` + tests T-E71

Le tunnel, s’il existe un jour, doit être **démarré sur le Mac** (ngrok / Tailscale / WireGuard) puis exposé comme hôte public dans `ARTCB_MAC_TUNNEL_SSH_HOST` (secret Cursor / Doppler **dev**, pas un RFC1918).
