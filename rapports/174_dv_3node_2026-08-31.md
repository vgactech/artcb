# Rapport 174 — validations 3 nœuds (D-034 AND, D-035/D-036)

**Horodatage UTC :** 2026-08-31T19:38:03Z  
**Branche :** `cursor/dv-3node-validation-16d8`  
**OVH2 / AWS3 :** `37d492aa4ba356aaba4d10ed2c51067ec904029a` `protocol_version=174-devnet-1`  
**OVH1 :** `5b4b24ae692ac2bb8255a4a5a3ca941b4365db29` — **non redéployé**  
**Sim :** `simulations/20260831T193803Z_e2e174_dv_3node` `failures=[]`  
**Certification :** **NOT MAINNET CERTIFIED**

Les V-01…V-07 tokenomics restent provisoires. D-033 (profil DV) n’est pas écrasé.

---

## Décisions (numérotation dépôt)

L’expert proposait D-033 A / D-034 B / D-035 B. **D-033 existait déjà** (profil DV). Gel réel :

| ID | Lettre | Contenu |
|----|--------|---------|
| D-034 | **A** | Hybride = Ed25519 **AND** ML-DSA-65. Carte signée liée à l’empreinte KEM. PQC non signé non mémorisé. |
| D-035 | **B** | Tous les tests possibles sur 3 nœuds. NODE4 plus tard. |
| D-036 | **B** | OVH1 reste sur `5b4b24ae` pour DV-03 inter-version. |

---

## Probe live

| Nœud | SHA | proto | HTTP/HTTPS | PQC | height | last_hash (après PRE-DV-04) |
|------|-----|-------|------------|-----|--------|-------------------------------|
| OVH1 | `5b4b24ae…` | champs absents → `legacy_missing_protocol_fields` | 200/200 | ML-DSA-65 | 1 | `8d542e49…` (inchangé) |
| OVH2 | `37d492aa…` | `174-devnet-1` compatible | 200/200 | ML-DSA-65 AND | 2 | `992f79df…` |
| AWS3 | `37d492aa…` | `174-devnet-1` compatible | 200/200 | ML-DSA-65 AND | 2 | `992f79df…` |

Carte `capability_card` signée : OVH2 et AWS3 **oui**. OVH1 **non** (code ancien).

---

## Verdicts DV

| ID | Choix | Verdict live | Preuve |
|----|-------|--------------|--------|
| DV-01 | C | **PENDING** | Identités crypto présentes ; OVH1 placeholder `artcb1_REMPLACER…` ; TPM pas exigé maintenant |
| DV-02 | C | **PARTIAL** | 3× HTTP/HTTPS 200 ; invalid kem 422 ; timeout TEST-NET-1 ; delete+readd 200 ; pas de flood/partition |
| DV-03 | B | **PARTIAL** | OVH2=AWS3=`artcb-devnet-1` + `174-devnet-1` + `genesis-artcb-v2`. OVH1 **REJECT** champs manquants (attendu D-036) |
| DV-04 | C | **BLOCKED** | 3/4 machines |
| PRE-DV-04 | — | **PASS** | TX publique OVH2 → sync AWS3 ; `last_hash` et `public_state_digest` **identiques** ; OVH1 non muté |
| DV-05 | C | **BLOCKED** | Aucun moteur BFT live. Extraite du code : quorum sim `n//2+1`, finalité sim N=2. `N≥3F+1` **non implémenté** |
| DV-06 | B | **PARTIAL** | timeout pair mort ; redémarrage **processus** OVH2 → height 2 conservée. Pas d’OFF VM / latence WAN injectée |
| DV-07 | C | **PARTIAL** | `hybrid_verify_mode=AND` live ; tests unitaires AND / anti-downgrade / expiration 2027 / carte signée. Fenêtre Ed25519 encore ouverte |

---

## Corrections de code

1. Annonce crypto liée à `sha256(kem_public)` + signature Ed25519 handshake (pas seulement `peer_id`).
2. Import de bloc public distant : hash+index+prev_hash, **pas** la clé de chaîne locale.
3. Extension de tip **uniquement** depuis un pair `protocol_compatible` (OVH1 legacy n’écrit pas la chaîne AWS3/OVH2).

---

## 27 pytest 170–174 (local). PR GitHub : `must be a collaborator`.
