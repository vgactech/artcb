# 269 — Campagne PBFT P9…P18 (pas HPC)

Date code : **2026-09-08**. Live à mesurer après follow-main sur le SHA de ce commit.  
AWS : **t3.small actuel uniquement**. Rapport HPC 269 **non autorisé** (PR #71 fermée).

## Code

- PREPARE refusé si digest ≠ PRE-PREPARE accepté (`not_accepted`).
- Chargement disque : certificats / prepared **non vérifiés sont ignorés**.
- Agent : `idempotency_conflict` si même `event_id` et payload différent ; `agent_identity_conflict` si même `agent_id` et owner/provider différents.
- `pbft_cert` reste un **sidecar** du hash (documenté + testé).

pytest `tests/test_e2e269_pbft_campaign.py` : 35 tests PBFT 266/268/269/265.

## Live

`scripts/run_live269_pbft_campaign.py` — JSON `logs/269_p9_p18_*.json`.

## Verdict (avant live)

Campagne unitaire : **PASS**. Certification live : **en attente du SHA déployé**.  
Preuve formelle de toutes les exécutions : **NON**.
