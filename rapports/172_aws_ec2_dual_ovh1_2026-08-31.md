# Rapport 172 — Instance AWS aws-node-3 + probe dual OVH1 (pré-lancement)

**Horodatage UTC :** 2026-08-31T16:20:00Z  
**Branche :** `cursor/aws-node-3-ec2-launch-16d8`  
**origin/main au départ :** `d092b61` (171)  
**Live OVH1 `/health` :** `5b4b24ae692ac2bb8255a4a5a3ca941b4365db29` branch=`main` — **≠** origin/main  
**Aucune VM OVH-2.**  
**Aucun token, mot de passe, access key n’est reproduit ici.**  
**Certification :** `READY FOR NEXT TEST` — **NOT MAINNET CERTIFIED**.

L’utilisateur a collé de nouveau le mot de passe console IAM et le secret d’application OVH-2 dans le chat. **Les rotator.** Ils ne sont pas dans git.

---

## A. Gap live / main (PROBE LIVE, pas inventé)

```
origin/main     d092b61   feat(171) bind Doppler + IAM gate
cette branche   (HEAD 172)
live OVH1       5b4b24ae  branch=main
```

Pas de redéploiement du nœud 1 (pas d’ordre deploy `:34`).

---

## B. IAM AWS — débloqué (PROBE LIVE)

Compte `599128160879`, user `node_artcb_3_agent`, région `eu-west-3`.

Secrets Cursor (aliases, pas les noms standard) :

- `AWS_API_KEY_AGENT_3` → `AWS_ACCESS_KEY_ID` (forme AKIA, 20)
- `AWS_API_CLI_AGENT_3` → `AWS_SECRET_ACCESS_KEY` (40)

Politiques attachées (IAM API, noms seulement) :

- `AdministratorAccess`
- `AmazonEC2FullAccess`
- `IAMFullAccess`
- `IAMUserChangePassword`

STS `get-caller-identity` : ARN `arn:aws:iam::599128160879:user/node_artcb_3_agent`.  
`ec2:DescribeInstances` : `[]` (aucune instance avant 172).  
VPC défaut `vpc-0b4c8f3e209ac6f51`, subnets publics `eu-west-3a/b/c`.

171 avait raison **à son époque** (ChangePassword only, pas d’access keys sous les noms standard).

---

## C. OVH 2 — toujours 0 machine

Inventaire via coffre `artcb-2` (pas les CK OVH1 du coffre partagé / env Cursor).  
Attente validation Public Cloud. **Aucune VM créée.**  
IP `152.228.144.34` = nœud 1 uniquement.

---

## D. Code 172 (avant RunInstances)

| Fichier | Pourquoi |
|---------|----------|
| `scripts/provision_aws_ec2.py` | aliases Cursor, SG 22/8000/8443, IP publique, idempotent `--yes` |
| `scripts/deploy_aws.sh` | Doppler `artcb3`, TLS :8443, n’envoie pas OVH1 |
| `scripts/run_sim172_aws_ec2.py` | probe OVH1 + AWS3 + inventaire OVH2 |
| `src/artcb/node_registry.py` | notes IAM admin + allowlist aliases |
| `scripts/ovh_api_inventory.py` | creds OVH2 depuis Doppler `artcb-2` |
| D-031 | GO compute AWS-3 |
| tests `test_e2e172_aws_ec2.py` | pas de secrets |

Tokenomics D-024…D-027 **inchangées**. V-01…V-07 **toujours ⏳**.

---

## E. Lancement / deploy

Renseigné **après** push de la branche (l’instance `git fetch` a besoin d’`origin/<branche>`).

Voir section mise à jour en bas de ce fichier après `provision_aws_ec2.py --yes` + `deploy_aws.sh`.

---

## F. Certification (pré-lancement)

```
READY FOR NEXT TEST
NOT MAINNET CERTIFIED
```

2 nœuds compute visés (OVH1 existant + AWS3). Pas 4. OVH2 absent. Consensus distribué V-01…V-07 **non testé**.
