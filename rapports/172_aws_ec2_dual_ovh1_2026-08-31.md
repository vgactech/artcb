# Rapport 172 — Instance AWS aws-node-3 + probe dual OVH1

**Horodatage UTC :** 2026-08-31T16:25:00Z  
**Branche :** `cursor/aws-node-3-ec2-launch-16d8` SHA `0d3d432` (plus commit post-deploy)  
**origin/main au départ :** `d092b61` (171)  
**Live OVH1 `/health` :** `5b4b24ae692ac2bb8255a4a5a3ca941b4365db29` branch=`main` — **≠** origin/main  
**AWS3 `/health` :** `0d3d432cea363ea7fd47ecf8dad63543eec2a0db` branch=`cursor/aws-node-3-ec2-launch-16d8`  
**Aucune VM OVH-2.** OVH1 **non redéployé.**  
**Aucun token, mot de passe, access key, seed n’est reproduit ici.**  
**Certification :** `READY FOR NEXT TEST` — **NOT MAINNET CERTIFIED**.

L’utilisateur a collé de nouveau le mot de passe console IAM et le secret d’application OVH-2 dans le chat. **Les rotator.** Ils ne sont pas dans git.

Création de PR GitHub : **échec** « must be a collaborator » (comme 170/171). Branche poussée sur `origin`. Push `main` demandé par l’utilisateur (permission Cursor).

---

## A. Gap live OVH1 / main (PROBE LIVE, pas inventé)

```
origin/main     d092b61   feat(171)
cette branche   0d3d432+  feat(172)
live OVH1       5b4b24ae  branch=main   ← inchangé après 172
live AWS3       0d3d432   branch=cursor/aws-node-3-ec2-launch-16d8
```

---

## B. IAM AWS — débloqué puis instance lancée

Compte `599128160879`, user `node_artcb_3_agent`, région `eu-west-3`.

Aliases Cursor : `AWS_API_KEY_AGENT_3` / `AWS_API_CLI_AGENT_3` → `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY`.

Politiques : `AdministratorAccess` + `AmazonEC2FullAccess` + `IAMFullAccess` + `IAMUserChangePassword`.

| Champ | Valeur (publique) |
|-------|-------------------|
| instance_id | `i-085b74abd1aaf04ee` |
| public_ip | `51.44.222.232` |
| type demandé | `t3.large` |
| type obtenu | `t3.small` (fallback script) |
| AMI | `ami-0dc0edbc6f5a51a4e` (Ubuntu 24.04) |
| VPC | `vpc-0b4c8f3e209ac6f51` |
| subnet | `subnet-0cc079949f4f4c413` (`eu-west-3a`) |
| SG | `sg-05bf5521acde4af64` ports 22/8000/8443 |
| key pair | `artcb-aws-node-3` |

---

## C. OVH 2 — toujours 0 machine

`ovh_api_inventory.py ovh-node-2` via coffre `artcb-2` :

- `/me` **200** : nic `vc491276-ovh`, email `vgac4237@gmail.com`, state `complete`
- `/cloud/project` **200** : `[]`
- instances : `[]`
- IP `152.228.144.34` = nœud 1 uniquement

---

## D. Déploiement AWS3

`bash scripts/deploy_aws.sh 51.44.222.232 cursor/aws-node-3-ec2-launch-16d8`

- Doppler CLI 3.76.5 + `/etc/artcb/doppler.env` (artcb3, root 600)
- systemd `artcb.service` active, `doppler run` + uvicorn
- TLS self-signed `:8443` (IP SAN)
- `POST /setup/init-node` sur localhost AWS ; seed **locale 0600** sur l’instance, pas git
- Health public : `bootstrap_mode=false`, `status=healthy`
- PQC AWS : Ed25519 fallback (`liboqs` non installé). OVH1 reste ML-DSA-65.
- Frontend npm **non** buildé (node absent sur l’AMI) — API seule.

---

## E. Dual probe (2/4 machines)

Sim `simulations/20260831T162428Z_e2e172_aws_ec2_dual_probe` : **failures=[]**

| Nœud | HTTP :8000 | HTTPS :8443 | SHA | P2P |
|------|------------|-------------|-----|-----|
| OVH1 | 200 healthy | 200 | `5b4b24ae` | register AWS3 **200** |
| AWS3 | 200 healthy | 200 | `0d3d432` | register OVH1 **200** |
| OVH2 | — | — | — | 0 VM |

Après register : `peer_count=1` de chaque côté (`artcb-devnet-1`).  
Ceci n’est **pas** un consensus distribué V-01…V-07. Les SHA des deux nœuds **diffèrent** (OVH1 non redéployé). PQC différent (ML-DSA vs Ed25519).

Tests unitaires 170+171+172 : **16 passed**.

---

## F. Invariants

| ID | État |
|----|------|
| origin/main connu | PASS |
| OVH1 health HTTP+HTTPS | PASS **PROBE LIVE** |
| live OVH1 SHA = origin/main | **FAIL** (5b4b24ae ≠ d092b61 / 0d3d432) |
| AWS3 health HTTP+HTTPS | PASS **PROBE LIVE** |
| artcb-2 / artcb3 bound, pas Stripe | PASS |
| OVH2 sans VM | PASS |
| AWS EC2 lancée | PASS |
| 2 compute simultanés | PASS |
| 4 machines | **NON** (2/4) |
| V-01…V-07 | **NON TESTÉ** |
| tokenomics D-024…D-027 | inchangées |

---

## G. Certification

```
READY FOR NEXT TEST
NOT MAINNET CERTIFIED
```

Prochaines actions :

1. Rotater mot de passe console AWS + secret d’app OVH2 (recollés dans le chat).
2. Attendre Public Cloud sur `vc491276-ovh` **puis** ordre VM OVH2.
3. Optionnel : `liboqs-python` + nodejs sur AWS3 ; Elastic IP.
4. Redéployer OVH1 sur `origin/main` **seulement sur ordre explicite**.
5. `DOPPLER_PERSONAL_TOKEN` pour extraire OVH1 hors de `artcb-blockchain`.
