# Rapport 289 — AEP lossless (trace opérationnelle, pas thinking privé)

**Live** : stamp `20260909T183729Z` run `AR-289-20260909T183729Z`  
**SHA ×4** : `05d703022a949a61849516e1c8ed20a4e715d9a7` = origin/main (docs 288 merge PR #83)  
**Livre OVH1** : height **1133**, tip `cc0bf8a1053c7ae75c3c00b0a4ebf72da311efbf1f13e3164a95f159fc7d71b3`  
**Ledger** : `logs/289_aep_agent_run_20260909T183729Z.json` (16 events, tip `b46e5112…`)

R284 `AgentRunLedger` est une **base** (hash-chain). Ce n’est **pas** encore la provenance exhaustive inter-environnements.

## Mesure

| Profil | Verdict |
|--------|---------|
| `mac_ssh_probe` | **COMPLETE_FOR_PROFILE** |
| SSH / LAN | **FAIL** / NOT_REACHABLE |
| `exhaustive_agent` (Cursor tools, FILE_WRITE, ARTCB_SEND, …) | **PARTIAL** |
| thinking_recorded | false |
| CERTIFIED_100 | false |

SSH FAIL + COMPLETE_FOR_PROFILE = la sonde Python a enregistré lookup Doppler, HTTP 400 `prd`, timeouts `:22`/`:8001`, décision NOT_REACHABLE. Ce n’est **pas** la session Cursor entière.

R284 `AgentRunLedger` est une **base** (hash-chain). Ce n’est **pas** encore la provenance exhaustive inter-environnements.

`docs(288)` sur `origin/main` (`05d7030`) = merge PR #83 (tunnel RFC1918), pas cet AEP.

## Ce qui est du code (pas seulement un rapport)

- `src/artcb/trace/agent_run.py` — hash-chain ; thinking **refusé** dans `detail`
- `src/artcb/trace/aep.py` — catalogue d’événements, `LOSSLESS_INPUT_POLICY`, `certify_provenance`
- `scripts/run_live289_aep.py` — ledger du probe Doppler/SSH/RFC1918

## Ce qui est prouvé vs prévu

| Fonction | État |
|----------|------|
| Hash-chain Hᵢ = SHA256(eventᵢ \|\| Hᵢ₋₁) | **Présent** (R284 + R289) |
| Thinking privé | **Jamais** (interdit) |
| R284 log 6 events = exhaustive | **NON** — `PARTIAL` |
| Probe Python `mac_ssh_probe` | mesuré live (COMPLETE_FOR_PROFILE possible **avec** SSH FAIL) |
| Cursor / ChatGPT / Claude tool traces | **NOT_PROVEN** |
| E2E agent → ARTCB (ingest 409) | **NOT_PROVEN** |
| CERTIFIED_100 | **false** |

SSH FAIL + provenance COMPLETE_FOR_PROFILE = « on sait pourquoi ça a échoué ». Ce n’est pas CERTIFIED_100.
