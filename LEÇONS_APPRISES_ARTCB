# LEÇONS APPRISES — ARTCB

**Horodatage :** 2026-07-04T19:55:00Z

---

## 2026-07-04 — Phase spécification

### L-001 — Dépôt documentation-only au départ
**Contexte :** Le dépôt ne contenait que des fichiers texte, aucun code source.  
**Leçon :** Toujours vérifier la présence de code avant d'estimer l'avancement.  
**Action :** CAHIER_DES_CHARGES_ARTCB créé pour cadrer l'implémentation.

### L-002 — Contradiction blockchain vs RT-LEG
**Contexte :** IDÉE_ARTCB propose DAG event-sourced ; PROTOCOLE exige blockchain 100 % décentralisée.  
**Leçon :** Les deux ne sont pas mutuellement exclusifs — blockchain light pour persistance + RT-LEG pour exécution.  
**Action :** Architecture hybride spécifiée dans CDC § 3.2.5.

### L-003 — Fichiers placeholder vides
**Contexte :** ROADMAP, STANDARD_NAMES, STRUCTURE, LEÇONS ne contenaient qu'une ligne.  
**Leçon :** Un dépôt avec placeholders crée une fausse impression de structure.  
**Action :** Tous remplis lors de cette session.

### L-004 — Incohérence noms fichiers PROTOCOLE
**Contexte :** PROTOCOLE cite `STANDARD_NAMES.md` mais le fichier est `STANDARD_NAMES_ARTCB`.  
**Leçon :** Documenter l'écart dans STANDARD_NAMES § 1.  
**Action :** Note ajoutée, pas de renommage pour ne pas casser l'historique.

### L-005 — Scope hackathon vs vision long terme
**Contexte :** Vision = OS mémoire IA + blockchain mondiale ; deadline = 15h.  
**Leçon :** MVP Avancé doit prouver le concept minimal (réversibilité + graphe + PoL), pas la vision complète.  
**Action :** P0/P1/P2 définis dans CDC § 4 et ROADMAP.

### L-006 — Piste Cursor = UX obligatoire
**Contexte :** Le projet était 100 % backend/théorie.  
**Leçon :** 50 % du jugement = démo qui fonctionne avec parcours utilisateur.  
**Action :** § 9 CDC — parcours 60 secondes spécifié.

### L-007 — Critique « redécouverte » des fondations
**Contexte :** Analyse externe : tokenisation, Huffman, embeddings existent déjà.  
**Leçon :** La différenciation n'est pas la compression mais la **réversibilité 100 % + traçabilité causale + PoL mesurable**.  
**Action :** Positionnement clarifié dans CDC § 1.2 et § 2.1.

### L-008 — SSH requis pour push
**Contexte :** Remote HTTPS avec token limité ; clé SSH ajoutée le 4 juil. 2026.  
**Leçon :** Configurer SSH dès le début pour éviter les limitations de push.  
**Action :** Clé `cursor-cloud-agent-vgac2025-lvx` configurée.

### L-009 — Audit v1.1 pré-développement
**Contexte :** Demande utilisateur de relire tout, combler les trous, ne pas lancer le dev.  
**Leçon :** 12 trous documentaires identifiés (WebSocket, fallback LLM, .artcb, INDEX, CONFIG…).  
**Action :** CDC v1.1 §20-26 + 5 nouveaux fichiers + rapport `000_audit_complet.md`.

### L-010 — main en retard vs feature branch
**Contexte :** CDC complet sur `cursor/cahier-des-charges-mvp-1fce`, pas sur `main`.  
**Leçon :** Les juges hackathon consultent souvent `main` par défaut.  
**Action :** Q-001 dans QUESTIONS_OUVERTES — merge recommandé avant dev.

### L-011 — Gate développement explicite
**Contexte :** Utilisateur a demandé d'attendre ses ordres avant le dev.  
**Leçon :** CHECKLIST_PRE_DEV_ARTCB §5 formalise le déblocage.  
**Action :** Développement lancé Phase 1 le 4 juil. 2026 sur ordre utilisateur.

### L-012 — Bug découpage de phrases (Phase 1)
**Contexte :** Tests montraient 1 seul nœud pour textes multi-phrases.  
**Leçon :** L'espace ne doit pas figurer dans le set de ponctuation fermante post-point.  
**Action :** Fix `_find_sentence_end` dans encoder.py — 20/20 tests passent.

### L-013 — Décisions utilisateur (4 juil. 2026)
**Contexte :** Réponses partielles aux QUESTIONS_OUVERTES.  
**Leçon :** Q-002/Q-004/Q-009 nécessitaient des explications plain-language, pas du jargon.  
**Décisions actées :** Merge auto main (D-001), blockchain C only (D-002), Python orchestration (D-003), EN code / FR rapports (D-004), MVP max (D-005).  
**Action :** DECISIONS_UTILISATEUR_ARTCB + QUESTIONS_OUVERTES v2.

### L-016 — Q-006 perdu puis corrigé (4 juil. 2026)
**Contexte :** Rapport 002 indiquait exemple CDC ; utilisateur exige livre Wailly 100%.  
**Leçon :** Toujours synchroniser DECISIONS + DEMO_TEXTE quand l'utilisateur tranche Q-006.  
**Action :** D-010 + `DEMO_TEXTE_ARTCB` + `ARTCB_DEMO_BOOK_PDF`.

### L-019 — Cloud Agent ≠ machine utilisateur (4 juil. 2026)
**Contexte :** Utilisateur exige exécution sur SON PC ; rapports 011 parlaient de « local » sur `/workspace` (VM cursor).  
**Leçon :** L'agent Cloud **ne peut pas** exécuter sur le disque de l'utilisateur. Distinguer `CLOUD_AGENT` vs `USER_MACHINE` via `logs/machine_fingerprint.txt`.  
**Action :** `EXECUTION_MACHINE_UTILISATEUR.md`, rapport `012`, détection hostname dans `run_real_local.sh`.

### L-018 — Démo ≠ ouvrir le frontend (4 juil. 2026)
**Contexte :** Utilisateur refuse toute fenêtre locale ; ne connaît pas l'UI.  
**Leçon :** La validation PROTOCOLE se fait via **script HTTP + logs** ; le frontend est optionnel (Phase 4), jamais imposé. Décrire l'UI en texte si l'utilisateur ne veut pas ouvrir de navigateur.  
**Action :** Rapport `010`, `make demo` sans frontend, tmux frontend arrêté.

### L-017 — Démo live : logs avant rapport (4 juil. 2026)
**Contexte :** Demande utilisateur de tout voir fonctionner avec logs réels.  
**Leçon :** Le PROTOCOLE exige de lire les logs *après* exécution avant d'écrire le rapport ; un script `demo_live.py` (httpx → API) produit des traces JSON + texte reproductibles.  
**Action :** `scripts/demo_live.py`, rapport `009`, vérification C `verify_chain_file` post-run.

### L-015 — Tokenomics PoL collectif vs Bitcoin (4 juil. 2026)
**Contexte :** Utilisateur propose 21M coins, minage d'apprentissage, récompense collective (pas winner-takes-all Bitcoin). Question sur testnet3/4.  
**Leçon :** Bitcoin testnet ≠ ARTCB — protocoles incompatibles. La différenciation économique = **split PoL proportionnel**, pas copier PoW. Nuancer : pools Bitcoin redistribuent ; PoL collectif ≠ zéro consensus.  
**Action :** `TOKENOMICS_ARTCB`, `RESEAU_DEVNET_ARTCB`, CDC v1.2 §27-28, D-014→D-018, rapport `005`.


## 2026-07-31 — Session Doppler + PQC Wallets + E2E Logger

### L-023 — Flux PoL /store requiert graph_id (PAS text direct)
**Contexte :** Le logger e2e appelait `POST /store` avec `text` directement → 422 "graph_id field required".  
**Leçon :** Le flux correct est TOUJOURS : `POST /encode` (text → graph_id) → `POST /store` (graph_id). `/mining/pipeline` est l'exception — il accepte `text` directement.  
**Action :** Documenter dans API_REFERENCE_ARTCB + auto-encode à ajouter dans /store (Phase 12.5.2).

### L-024 — Anti-sybil rate-limit bloque l'API entière (synchrone)
**Contexte :** Après `POST /store`, toutes les routes API suivantes timeoutaient à 8s. Root cause : FastAPI synchrone — le handler /store attend le rate-limit anti-sybil (60s) et bloque le thread.  
**Leçon :** Ne jamais faire d'attente synchrone dans un handler FastAPI. Utiliser `asyncio.sleep` ou déporter dans un background task.  
**Action :** Phase 12.5.1 — rendre /store et /mining/pipeline async (urgent P0).

### L-025 — Wallet e2e dédié pour les tests (ne jamais utiliser vgactech)
**Contexte :** Le logger e2e utilisait `vgactech` → taux limite déclenché → toutes les routes suivantes bloquées.  
**Leçon :** Créer un wallet frais à chaque run e2e (`e2e_run_{TIMESTAMP}`). Jamais réutiliser un wallet de production en test.  
**Action :** `E2E_WALLET = f"e2e_run_{TIMESTAMP}"` dans `scripts/e2e_logger.py`.

### L-026 — Doppler : pas de projet ARTCB préexistant
**Contexte :** 5 projets existaient sur Doppler (debugai, lumvorax, magen-arc-agi, mdbai, tradelvx) — aucun ARTCB.  
**Leçon :** Vérifier Doppler AVANT d'assumer que le projet existe. Créer avec `doppler projects create`.  
**Action :** Projet `artcb-blockchain` créé + 24 secrets dev/stg/prd synchronisés.

### L-027 — 32 wallets legacy sans PQC (bench_w* + vgactech + miner_demo)
**Contexte :** 12/44 wallets seulement étaient hybrides. Les 32 anciens étaient Ed25519 pur — cassables par Shor.  
**Leçon :** La migration PQC doit être appliquée à TOUS les wallets existants, pas seulement les nouveaux.  
**Action :** Script de migration one-shot — 44/44 wallets maintenant hybrides Ed25519+ML-DSA-65.

### L-028 — PDF Wailly contient une boucle auto-référente (LimitReachedError)
**Contexte :** `test_async_pdf_extraction` échouait sporadiquement avec `LimitReachedError` dans pypdf.  
**Leçon :** `LimitReachedError` est une sous-classe de `PdfReadError` mais doit être attrapée explicitement. Aussi attraper `Exception` générique à tous les niveaux (reader init + page extract).  
**Action :** `except (PdfReadError, LimitReachedError, Exception)` aux 3 endroits + test assoupli.

### L-029 — Tokenomics 1/105k ≠ 21 M (25 août 2026)
**Contexte :** Audit simulation vs `main` : `INITIAL_BLOCK_REWARD=1`, `HALVING_INTERVAL=105_000` ⇒ plafond asymptotique 210 000 ARTCB, alors que D-014 annonce 21 M. D-016 disait déjà 210 000 blocs.
**Leçon :** Un hard cap documenté n'est réel que si `R_0 × intervalle × 2` l'atteint. Toute réduction du reward initial doit recalculer l'intervalle (ou l'inverse).
**Action :** Rapport 124 — R_0=50, intervalle=210k, couche R(H)/HBP/P_owner continue. Ne plus mélanger le calendrier 1/105k avec le modèle validé.

### L-030 — Dumps de conversation hors ordre + spec qui bouge après le code (26 août 2026)
**Contexte :** Fichiers `rapports/125`…`159` sont des copier-coller ChatGPT ; 140≡141, 154≡155 ; 159 contient 3 sessions. Le code `25b832f` implémente 153–155. Les rapports 158–159 abrogent ensuite 210k, la vélocité et le settlement par index.
**Leçon :** (1) Lire les dumps dans l’**ordre de discussion**, pas l’ordre des numéros. (2) Un « je verrouille » ChatGPT ≠ D-xxx tant que DECISIONS n’est pas amendé. (3) 535 pytest verts figent le modèle 124, pas le dernier mot conversationnel. (4) Ne pas patcher 159 tant que M1-après-M2 et H_adult,max ne sont pas tranchés par l’utilisateur.
**Action :** Rapport 160 — inventaire questions / code / manques. Pas d’implémentation 158–159 dans cette passe.

### L-031 — EconomicRoot hors du hash de bloc
**Contexte :** `ffi.build_block_hash` canonise index/timestamp/prev/graph/merkle/pol **avant** d’écrire `economics` et `contributors` réécrits par `settle_block` (rapport 156 §14, confirmé lecture `chain/manager.py`).
**Leçon :** Un settlement JSON non haché n’est pas du consensus. Changer le canonique C est un fork.
**Action :** Noté 160 §6.12 ; attendre GO + spec des champs.

### L-032 — D-024 retrait halving 210k (26 août 2026)
**Contexte :** L'utilisateur confirme la géopopulation et exige de retirer tout ce qui est lié au halving 210 000 blocs sans casser le reste.
**Leçon :** Un calendrier Bitcoin `R0 >> (index//H)` n'est pas compatible avec `R = f(H)`. Le garder « en plus » (min(schedule, R(H))) recoupe encore le reward au bloc 210k. Il faut le sortir du chemin live, pas seulement le documenter. La vélocité `extra_epochs` était un second halving : même retrait. 21 M reste un cap, plus une identité 50×210k×2 d'émission.
**Action :** Rapport 161. Tests : index 210k = toujours 50 si H≤1M. Archive `DEPRECATED_HALVING_INTERVAL`.

### L-033 — OwnerDecay flotte + time-norm + vault vs remaining (28 août 2026)
**Contexte :** Dump 162. L’utilisateur tranche M1=100 % pour toujours et « même P pour toutes les extras » (49 % @N=3, ~48 % @N=4), pas la courbe index 38 %@1000. Il exige aussi : 10× plus de blocs ⇒ 1/10 reward (sans 210k). Frais → UniversalDividendVault, ce qui **contredit** 161 remaining-supply.
**Leçon :** (1) P(n_index) ≠ P(N_economic). (2) Un reward/bloc fixe avec un intervalle variable épuise 21 M (48,6 j @10 s). Normaliser par T déjà documenté (600 s) n’est pas un halving. (3) Dernier GO utilisateur > spec 161 sur les frais. (4) k se **dérive** des exemples (P(3)=49 %), on n’invente pas un 3e ancrage. (5) Burst tests <1 s doivent filtrer dt sinon time-norm casse 50+25 ARTCB.
**Action :** Rapport 163, D-025, `fleet_owner_share` / `issued_reward_satoshi(..., actual_block_interval_seconds)`.

### L-034 — Câblage e2e ≠ modules isolés (28 août 2026, sim 164)
**Contexte :** Audit croisé : ~82 % des modules economics existent, mais mining/API tombaient encore en PoL 50/50 héritage faute de `machine_index` / `HumanID` / `WorkID` sur les contributors. EconomicRoot était mixé dans merkle Python, pas dans le canonique C. OVH healthy n’implique pas « cette branche est déployée ».
**Leçon :** (1) Un module `settle_block` n’est du consensus que s’il est dans `append_block` **et** dans le hash C. (2) Versionner le hash (v1 sans root, v2 avec) plutôt que casser les blocs anciens. (3) Ne jamais copier BTC/USDT comme prix ARTCB. (4) JobPayment Stripe ≠ mint. (5) Doppler token 401 et SSH sans clé = documenter, pas inventer un token.
**Action :** Rapport 164. `ProtocolEngine`, ABI C v2, oracle stub honnête, workflow `stripe-priority.yml`.

### L-035 — HumanID ≠ adresse de paiement (28 août 2026)
**Contexte :** `_machine_contributions` utilisait `bound_human_address or human_id`. M1 (pas d’humain bindé) recevait `human_id=H-A` comme destinataire HBP. ProtocolEngine et `append_block` calculaient deux EconomicRoot différents (2 vs 3 lines).
**Leçon :** HumanID est une identité ; le settlement ne paie que des **wallets**. Un fallback « or human_id » est une fuite de satoshi hors carnet d’adresses.
**Action :** `bound_human_address` seul ; sim 164 `20260828T200518Z` roots identiques.

### L-036 — Régénérer Doppler casse le nœud OVH (29 août 2026)
**Contexte :** Token Cursor mis à jour et valide. Token dans `/etc/artcb/doppler.env` (root 600) resté l’ancien → `doppler run` « Invalid Auth token » → crash loop après `systemctl restart`. Doppler `OVH_SERVER_IP` ≠ IP live. Secret Stripe Cursor = `KEY_API_STRIPE`, pas `KEY_API_STRIPE_ACTION`. Clé SSH = `SSH_PRIVATE_KEY` Doppler, pas `OVH_SSH_PRIVATE_KEY`.
**Leçon :** (1) Un service token par usage (Cursor / Replit / serveur). (2) Après rotation, mettre à jour le fichier serveur ou le nœud meurt au prochain restart. (3) `doppler me` avant `doppler run`. (4) Ne jamais prendre `OVH_SERVER_IP` Doppler comme cible de deploy sans vérifier le ping SSH. (5) Noms de secrets : vérifier `CLOUD_AGENT_INJECTED_SECRET_NAMES` avant de conclure « absent ».
**Action :** Rapport 166. `start_node.sh` preflight. `load_ovh_ssh_from_doppler.py`.

### L-037 — Snapshot + WorkID global, pas seulement SettlementID (29 août 2026)
**Contexte :** Sim 167. (1) Transfert de M4 vers B sans M1 chez B → `first machine cannot bind third-party`. (2) `taken_at` par nœud → SID différents pour le même WorkID → 4 settles possibles.
**Leçon :** Un transfert vers un owner sans flotte crée un M1. L’unicité doit porter sur **WorkID réseau**, pas seulement hash(snapshot). Un timestamp non aligné casse la reproductibilité du SID.
**Action :** Rapport 167. Ledger `consume` refuse le même WorkID. Epoch aligné. MB1 pré-enregistré.

### L-038 — Clé agent = login nœud, pas un secret inventé (29 août 2026)
**Contexte :** Demande d’utiliser le nœud live à chaque prompt. 0 wallet / 0 clé sur OVH. `generate` exige `sess_`. Doppler `PUT` secrets = 404 ; `POST` = OK. `ArtcbClient()` / MCP lisaient l’env live et cassaient les tests localhost.
**Leçon :** (1) Provisionner sur le nœud (wallet + hash de clé). (2) Stocker dans Doppler + fichier 600, jamais dans le chat. (3) Bootstrap obligatoire. (4) Isoler `ARTCB_API_URL` dans les tests. (5) Le nœud peut rester en 166 alors que `main` est en 167 — le health `git_sha` prime.
**Action :** Rapport 168. `artcb_live_bootstrap.py`. D-027.


