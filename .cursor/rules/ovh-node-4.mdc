---
description: OVH node artcb 4 — GRA11, isolated Doppler artcb-4
alwaysApply: false
---

# OVH — nœud artcb 4

- nic `xy4589-ovh` (`vgac42@gmail.com`). Public Cloud `926bb1d6755e4f2c98ae9db06ef44e4f`. Instance `node-artcb-ovh-4` id `22dc6a47-5b79-4084-82d7-eabb4f5b2680` GRA11 d2-8 IPv4 **`91.134.45.8`**.
- Doppler : projet **`artcb-4`** (UI `ARTCB_API_4`). Token Cursor **`KEY_API_ARTCB_DOPPLER_4`**. Jamais Stripe/Bob/GitHub ici. Jamais les `OVH_*` du process Cursor (nœud 1).
- SSH : `ubuntu@91.134.45.8`, clé locale `~/.ssh/artcb_ovh_node_4` (Doppler `SSH_PRIVATE_KEY`). Publique git `deploy/artcb_ovh_node_4.pub` comment `artcb-ovh-node-4-20260902`.
- Health `http://91.134.45.8:8000` ; API `https://91.134.45.8:8443` ; domaine `https://n4.artcb.me/health`.

## Interdits

- Pas de **rescue** OVH4. Pas de `reinstall`. Pas de wipe `blocks.jsonl`.
- Ne **pas** coller un PEM / `dp.st.…` dans le chat. `ARTCB_OVH_NODE_4` Cursor est une clé **publique**.
- Les 4 nœuds officiels suivent `origin/main` via `artcb-follow-main.timer` (keep-book). Ne plus demander un redeploy à la main.
- Les jetons Doppler 1/2/3 **refusent** `artcb-4` (HTTP 400).

## Si `SSH_PRIVATE_KEY` manque dans Doppler

1. Générer ed25519, POST Doppler `artcb-4` / `dev` / `SSH_PRIVATE_KEY`.
2. Poser la publique **sans rescue** : Nova `changePassword` (qemu-ga, user `root` sur l’image Ubuntu) + VNC instance **ACTIVE** (`POST .../instance/{id}/vnc`), puis `authorized_keys` ubuntu. Relocker root (`passwd -l`) après SSH ubuntu.
3. Keep-book `origin/main` par **bundle** si `git fetch origin` HTTPS n’a pas d’identifiants.
